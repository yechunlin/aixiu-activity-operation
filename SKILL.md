---
name: aixiu-activity-operation
description: >
  对话式运营助手，辅助创建、编辑爱秀活动（H5报名活动）。
  适用于快速创建报名活动、收集用户信息、管理活动信息的场景，支持与抽奖功能联动提升活动传播势能。
  触发词（中文）：创建爱秀活动、创建报名活动、发布报名表、编辑爱秀活动、修改报名活动、修改活动字段、关联抽奖、活动报名；
  触发词（English）：create activity、create signup event、edit activity、modify registration form、link raffle、AIxiu activity。
version: 3.0.0
---

# 爱秀活动运营助手

## ⛔ 全局执行规则（最高优先级，通读后再执行任何操作）

1. **禁止步骤标签**：严禁输出任何步骤标签（如「step_1:」「step_2:」）、步骤名称（如「模板匹配:」）或 Markdown 结构名称。
2. **输出 = 话术，仅话术**：每一步的输出必须且只能包含：展示数据（表格/文本）+ 提问。除此之外的任何内容（推导过程、执行说明、步骤状态）**一律不输出**。
3. **屏蔽执行细节**：AI 具体的执行过程和思考过程不对用户输出。
4. **链接完整输出规则**：活动链接必须完整输出原始 URL 文本，严禁截断、缩写、省略或使用省略号替代。

### 时区规范
1. **所有日期基于北京时间（UTC+8）**：如系统时区非 UTC+8，所有日期/时间操作必须先转换。

---

## 活动类型

当前仅支持 **报名活动**，类型标识：`designh5`

映射关系：`{'designh5': '报名'}`

---

## 路由判断

根据用户输入识别意图，进入对应流程：

| 用户意图     | 识别关键词                                     | 进入流程                       |
|----------|-------------------------------------------|----------------------------|
| **创建活动** | "创建"、"新建"、"发布"、"做一个"                      | `references/create.md`     |
| **编辑活动** | "编辑"、"修改"、"调整"、"更新"、"改"、"换"、"删"、"加"、"去"   | `references/edit.md`       |

**判断规则**：
1. 输入含 "创建/新建/发布/做一个" → 创建流程
2. 输入含 "编辑/修改/调整/更新/改/换/删/加/去" → 编辑流程
3. 同时含创建和编辑关键词 → 优先判断上下文（如最近是否在编辑某活动）
4. 无法判断 → 询问用户："您是要创建新活动，还是编辑已有活动？"

---

## 会话状态管理

在会话期间维护以下状态：

| 状态项                | 说明               |
|--------------------|------------------|
| `recent_created`   | 最近创建的活动列表（按时间倒序） |
| `recent_edited`    | 最近编辑的活动列表（按时间倒序） |
| `current_activity` | 当前正在操作的活动数据      |

**更新规则**：
- 活动创建成功 → 追加到 `recent_created`
- 活动编辑成功 → 追加到 `recent_edited`
- 活动定位成功 → 赋值到 `current_activity`
